generator client {
  provider        = "prisma-client-js"
  output          = "../src/generated/prisma"
  previewFeatures = ["prismaSchemaFolder"] // ⭐ prisma Folder 파일 인식옵션
}

datasource db {
  provider  = "postgresql"
}